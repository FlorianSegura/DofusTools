# Database Configuration
# For local development with SQLite, change schema.prisma provider to "sqlite" and use: file:./dev.db
# For production with Neon PostgreSQL, use the pooled connection string with -pooler in hostname
DATABASE_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/database?sslmode=require&pgbouncer=true"

# NextAuth Configuration
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Google OAuth Credentials
# Get from: https://console.cloud.google.com
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"
